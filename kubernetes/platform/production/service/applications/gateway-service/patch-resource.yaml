apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
spec:
  template:
    spec:
      containers:
        - name: gateway-service
          resources:
            requests:
              memory: 756Mi
              cpu: "1"
            limits:
              memory: 2G
              cpu: "2"
      